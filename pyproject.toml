[build-system]
requires = ["uv_build~=0.11"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = 'textual_nav'
module-root = ''

[project]
name = "textual-nav"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9,<4.0"
dependencies = [
    "textual-dev>=1.8.0",
    "textual[dev]>=8.2.8",
]
